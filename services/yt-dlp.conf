# Shared defaults for mix-saving-bot media downloads (YouTube, Instagram, TikTok).
# Loaded via YoutubeDL option config_locations; per-mode format/postprocessors are set in code.

--no-playlist
--retries 10
--fragment-retries 10
--socket-timeout 45
--restrict-filenames
--trim-filenames 120
--no-overwrites
